{
  "name": "navifare-flight-results-widget",
  "version": "1.0.0",
  "description": "React component for displaying flight price results in ChatGPT",
  "main": "dist/component.js",
  "scripts": {
    "build": "esbuild src/component.tsx --bundle --format=esm --outfile=dist/component.js --jsx=automatic",
    "dev": "esbuild src/component.tsx --bundle --format=esm --outfile=dist/component.js --jsx=automatic --watch"
  },
  "keywords": ["react", "chatgpt", "mcp", "flight", "travel"],
  "author": "Navifare",
  "license": "MIT",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.1",
    "esbuild": "^0.25.10",
    "typescript": "^5.9.3"
  }
}
